# Define some colors
BLACK = (0, 0, 0)
DARK = (40, 40, 40)
GREY = (80, 80, 80)
WHITE = (255, 255, 255)
GREEN = (0, 255, 0)
RED = (255, 0, 0)

# Set width and height of cell in the grid (in pixels)
CELL_WIDTH = 30
CELL_HEIGHT = 30

# Set number of rows and columns in the grid
NUM_ROWS = 12
NUM_COLS = 20

# Set margin between each cell (in pixels)
CELL_MARGIN = 1

# Set width and height of the screen
SCREEN_SIZE = [CELL_WIDTH * NUM_COLS, CELL_HEIGHT * NUM_ROWS]
